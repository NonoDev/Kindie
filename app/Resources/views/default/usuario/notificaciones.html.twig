
{% extends '::layout.html.twig' %}
{% block title 'Kindie | Mis notificaciones' %}
    {% block contenido %}
        <h1 class="center">Listado de notificaciones</h1>

        <div class="row notificaciones">
            <div class="col s12 m10 l8 offset-l2 offset-m1">
                {% if notificaciones %}
                <ul class="collection">
                   {% for noti in notificaciones %}
                    <li class="collection-item avatar">
                        <i class="mdi-action-assessment circle green"></i>
                        <span class="title">{{ noti.tipo }}</span>
                        <p>
                            {{ noti.descripcion }}
                        </p>

                        <a href="#" class="secondary-content"><i class="mdi-action-announcement
"></i></a>
                    </li>
                    {% endfor %}
                </ul>
            {% else %}
                <h4 class="center">No has recibido notificaciones aún</h4>
                {% endif %}
            </div>
        </div>
    {% endblock %}