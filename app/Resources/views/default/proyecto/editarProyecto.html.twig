{% extends '::layout.html.twig' %}
{% block title %}Kindie | Desarrollo del proyecto{% endblock %}
    {% block contenido %}
        <section id="editar-proyecto" class="responsive-img" data-type="parallax_section" data-speed="5">
        <h1 class="center">Edición de <strong>{{ proyecto.nombre }}</strong></h1>
            <div class="row">
                <div class="col s12 m8 l6 offset-l3 offset-m2">
                    {{ form(formulario) }}
                    <a href="{{ path('proyecto', {'id':proyecto.id}) }}" class="enlace-azul"><i class="mdi-navigation-arrow-back"></i> Vuelve al proyecto</a>
                </div>
            </div>

        </section>

    {% endblock %}