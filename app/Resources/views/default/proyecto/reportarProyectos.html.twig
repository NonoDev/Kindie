{% extends '::layout.html.twig' %}
{% block title %}Kindie | Reporte de proyectos{% endblock %}
    {% block contenido %}
        <section id="reportes" class="responsive-img" data-type="parallax_section" data-speed="5">
            <h1 class="center">Reporte del proyecto {{ proyecto.nombre }}</h1>
            <div class="row editar-perfil">
                <div class="col s12 m8 l6 offset-m2 offset-l3 ">

                    <!--<a href="{{ path('proyecto', {'id':proyecto.id}) }}" class="enlace-azul"><i class="mdi-navigation-arrow-back"></i> Vuelve al proyecto</a>
                    --><blockquote>Puedes reportar un proyecto siempre que incumpla cualquier norma de Kindie. Siempre que veas imágenes con contenidos inadecuados,
                    títulos ofensivos, con carácter racista, xenófobo o que puedan atentar contra la paz y buena convivencia de la comunidad de Kindie, no dudes en reportar.
                    No reportes sin necesidad, los usuarios que abusen de éste método de mejora de la convivencia sin motivo podrán ser castigados incluso pudiendo perder su cuenta.</blockquote>
                </div>
                <div class="row">
                    <div class="col s12 m8 l6 offset-l3 offsetm2">
                <form method="post" action="{{ path('reportar_proyecto', {'id':proyecto.id}) }}">

                    <div class="row">
                            <div class="row">
                                <div class="input-field col s12">
                                    <textarea id="textarea1" class="materialize-textarea" name="motivo" required="required"></textarea>
                                    <label for="textarea1">Tu motivo</label>
                                </div>
                            </div>
                    </div>

                    <button type="submit" name="enviar" class="btn blue">
                        Reportar
                    </button>
                </form>
                    </div>
                </div>
            </div>

        </section>

    {% endblock %}